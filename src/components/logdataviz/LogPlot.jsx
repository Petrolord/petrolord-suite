
import React from 'react';

const LogPlot = ({ logData, anomalies, curves }) => {
  return (
    <div className="w-full h-[600px] flex items-center justify-center text-slate-500 bg-white rounded">
      Chart removed
    </div>
  );
};

export default LogPlot;
