{
  "name": "@petrolord/engines",
  "version": "0.1.0",
  "private": true,
  "description": "Petrolord validated petroleum-engineering engines — pure client-side math, oracle-validated against committed goldens. Consumed by Petrolord Suite and Petrolord NextGen via git subtree vendoring.",
  "type": "module",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.24.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0"
  }
}
