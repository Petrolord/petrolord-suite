# Earth Modeling goldens (Phase G8.0)

Generated by `tools/validation/earthmodel/genfixtures.py` (stdlib-only,
deterministic — rerunning is byte-identical) from the independent
oracle `tools/validation/earthmodel/oracle.py`. Never edit by hand;
regenerate and let the anchors gate the result.

Reference basis (oracle written from primary definitions, never from
the JS):

- Bilinear resampling: tensor-product linear interpolation (Press et
  al., Numerical Recipes 3rd ed. §3.6), house null convention.
- Minimum curvature trajectories: ratio-factor formulation
  RF = (2/dogleg)·tan(dogleg/2) (Bourgoyne et al., Applied Drilling
  Engineering, SPE Textbook Vol. 2, ch. 8) — same formulation as the
  proven `src/lib/wellpath-kernel.js`.
- Least-squares plane trend: normal equations.
- Simple kriging: Isaaks & Srivastava, An Introduction to Applied
  Geostatistics ch. 12/16; honor-the-data covariance convention
  C(0) = sill, C(h>0) = (sill − nugget)·corr(h) (exact interpolator at
  data points), spherical + exponential models.
- Point-in-polygon: even-odd ray crossing (PNPOLY formulation).
- Volumes: cell-centred sums, thickness × cell area (the
  `grvAcreFt` convention in `src/lib/gridding/surfaceExport.js`).

Anchor set A1–A9 is asserted at generation (see the genfixtures
docstring): analytic-plane resampling exactness, monotonic-clamp
no-op, the 180-node pinch-out clamp + analytic thickness law,
hand-counted fault-block census {0: 326, 1: 174}, vertical-well
identity + exact 0→90° circular-arc build (ΔZ = ΔE = 2ΔMD/π), exact
planar trend recovery, kriging exactness at data / mean-at-infinity,
closed-form zone-A bulk volume (45,000,000 m³), and exact
position-at-station interpolation.

Consumed by `src/pages/apps/EarthModeling/__tests__/*` (jest) and the
`/dev/earth-modeling` Playwright harness.
