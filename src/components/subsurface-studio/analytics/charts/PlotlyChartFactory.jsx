
import React from 'react';

const PlotlyChartFactory = ({ style = {} }) => {
    return (
        <div className="w-full h-full min-h-[300px] flex items-center justify-center text-slate-500" style={style}>
            Chart removed
        </div>
    );
};

export default PlotlyChartFactory;
