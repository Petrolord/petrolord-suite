// Re-export shim (2026-09-03): the track layout editor moved to
// src/components/wells so Well Correlation shares it. Import the shared
// path in new code.
export { default } from '@/components/wells/LayoutPanel';
