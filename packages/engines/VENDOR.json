{
  "$comment": "Vendoring contract for packages/engines. Checked by tools/check-vendored-engines.mjs; see that file for the model. The check is against the PINNED commit below and its committed manifest, never against engines HEAD: a guard pointed at HEAD goes red the moment canonical moves and then blocks every unrelated Suite PR. Moving the pin is a deliberate, reviewed vendoring pass. The Suite vendors the WHOLE canonical tree, so the expected state of this file is the one it is in: an EMPTY knownDeviations list. Any deviation that is genuinely deliberate goes in that list with a reason, a group and a burnDownWhen, and every differing or extra path is pinned to the blob currently vendored, so a second drift on an already-known path still fails. burnDownWhen states the condition under which the row must be removed, because a bare reason goes stale on its own: in NextGen a row reading \"pull only with the recut in the same deploy window\" became false the moment the recut merged and nothing in the file said so. A ledger entry whose deviation has gone is also a failure, so the list can only shrink.",
  "canonical": {
    "repo": "git@github.com:Petrolord/petrolord-engines.git",
    "commit": "b43f1d9e6f7cb2cac8142959052b612834c5bc25",
    "manifest": "VENDOR.manifest"
  },
  "vendoring": {
    "method": "file-by-file reconciliation against a canonical clone, verified by blob hash with this guard, hashed from the files on disk rather than from the git index",
    "$comment": "NOT a whole-tree `git subtree pull`. That silently DELETED 24 Suite-only downstream files on 2026-09-14. Vendor the paths the canonical range actually touched, then run the guard, which is what proves nothing else moved.",
    "lastVendored": "2026-09-19",
    "lastVendoredRange": "a1d8c9fb8379edca921dd31fdc6c9ccec8a6ebd4..b43f1d9e6f7cb2cac8142959052b612834c5bc25 (engines PR #223, HSE H4 consequence modelling, for the Process Safety PS2 Consequence Modelling Studio; also carries engines #220 and #222, HSE exposure and safetyStats message and rationale fixes, and #221, MD3-0 terminals, depots and fuel supply, all merged meanwhile). 26 canonical paths (13 added, 13 modified). BEHAVIOUR CHANGES: new engines/hse/consequence.js (source terms, Gaussian plume, solid-flame pool fire, Kinney-Graham blast, probits; first Suite importer is PS2 through src/utils/processSafety/engine/consequence.js); engines/facilities/spacing.js now exports thomasFlameHeightM and poolFireSetbackM calls it with the same expression and defaults, bit for bit unchanged (FC1 setbacks and the facilities suites unchanged); exposure nioshHeatAssessment names the renamed per-period field in its refusal message (no figure moves); safetyStats pseRate's missing-base hint names only 200,000 and 1,000,000 (no figure moves); MD3-0 (#221): terminalDepot refuses a missing opening stock (it was 0), a dip below the first strapping entry, an unconvertible water cut and a non-integer or zero bay count, stops netting heel across tanks and weighs a loss with no density as refused; fuelPricing solves a percentage charge on a base not yet formed in closed form (insurance on CIF was charged on FOB) and refuses forward references, unknown stages and a blank trucking cost. The Suite Terminal & Depot and fuel supply pages are adapted in the MD3-0 Suite PR (branch fix/md3-0-supply-apps), which vendors the same #221 bytes. Previous range: ac450120d4cb0e55e84be7f72232c135f3060e7b..a1d8c9fb8379edca921dd31fdc6c9ccec8a6ebd4 (engines PR #219, MD2-0: oracles and repairs for refinery planning, the variance model and modular refinery feasibility; also carries engines #218, HSE LOPA, merged meanwhile: 6 added paths under engines/hse and tools/validation/hse, no Suite importer). 20 canonical paths (14 added, 6 modified). BEHAVIOUR CHANGES: planRefinery runs every barrel of crude through the feedless (crude) units by an equality row (at the Suite default the CDU now carries 2.91M bbl and the margin is 18.36M, was 21.85M with the CDU idle), refuses a blank crude cost, unit operating cost or product price, reads a typed 0 availability, capacity or maximum demand as 0 (it was unlimited) and a negative one as invalid, and unitRuns gain crudeUnit; cascadeToSchedule dates in UTC (it slipped a day in the Americas after the spring clock change); attributeVariance lines gain direction (revenue for deliveries, cost otherwise), marginEffect and unexplained, and total is now margin-signed with total.cost and total.revenue apart (it added revenue and cost gaps together); feasibilityStreams refuses blank capacity, crude cost, capital, fixed or variable opex, refuses a utilisation outside 0..1 (it clamped) and spends capex in year 0 when there is no construction period (it vanished); new feasibilityEconomics runs the streams through calculateEconomics with revenue as revenue, no royalty and lossCarryForward; screening.calculateEconomics gains opt-in lossCarryForward (default false; every economics golden unchanged) and a taxLossCarriedForward cashflow field. Previous range: 3709448bc8ad3af7cc3b03a0d6d69c6aae929a22..ac450120d4cb0e55e84be7f72232c135f3060e7b (re-pin to the engines #215 squash merge ac45012, which also carries engines #216 and #217 merged meanwhile by the HSE programme: 13 canonical paths, all added, engines/hse/safetyStats.js and engines/hse/exposure.js with their tests, goldens, oracles, FINDINGS and negative controls, plus README rows. No Suite file imports engines/hse yet, so no app behaviour changes from them; the #215 content is byte-identical to the PR head vendored in the previous range). Previous range: 5cbdca5fb78cf83771ede4119fb4a92cb7aacaa9..3709448bc8ad3af7cc3b03a0d6d69c6aae929a22 (engines PR #215, MD1-0: oracles and repairs for crude assay, product blending and the LP kernel; vendored from the PR head, then re-pinned here to the squash merge commit). 16 canonical paths (5 modified, 11 added: three golden gates, three goldens, three oracles, FINDINGS-crude.md, negcontrol_md1.sh). BEHAVIOUR CHANGES: solveLP drives zero-level artificials out after phase one (it returned points breaking their own rows and a false unbounded) and undoes the dual sign of a row negated to normalise its rhs, and reports real iterations; optimiseBlend refuses a component with no cost, a negative bound or a floor above its ceiling, reads a typed maxVolume of 0 as none (it was unlimited), skips a mass-basis spec when a stream has no density (it blended as SG 1), judges binding relative to the limit, and prices each spec's shadow row per unit of the property as money saved by relief (price) with the raw dual kept as rowPrice (it was the row dual, 0.072 against 55.01 per ppm at the default pool); crudeAssay volumePercentAt answers outside a curve only below a 0 percent first point or above a 100 percent last point and is null elsewhere (it clamped flat), cutYields returns null for an unknown or inverted cut and lists unknownCuts, blendCrudes refuses no gravity, a mixed volume and mass basis and negative shares and returns a partially supplied property as null with the crudes named in missing (it read blanks as zero), the stability screen returns stable null for the CII uncertain band and for the no-SARA fallback unless it raises its flag (it returned true), netbackValue names blank costs in assumedZero and refuses losses outside 0 to 100, d86ToTbp lists skipped differences; new blendDistillationCurves and temperatureAtVolumePercent. Previous range: ab3ce6af5cd64e3dba2fe1c94493602f57e0ccdb..5cbdca5fb78cf83771ede4119fb4a92cb7aacaa9 (engines PR #214, the facilities owner copy rule sweep). 19 canonical paths, all modified: nine engines/facilities modules, four facilities test suites and six FINDINGS files. BEHAVIOUR CHANGES: none numeric; 23 user-facing strings reworded to the owner copy rule, the producedWater media filter floor refusal now says the module answers at 1 m/hr and above it (the code always did), and two comments corrected (the droplet grid step is 9.8 percent a bin at the defaults; BAR_TO_PSIA keeps its value and is no longer called exact). Previous range: 9d5d3b486493b8d08eb7dc38557d10e63e56adc2..ab3ce6af5cd64e3dba2fe1c94493602f57e0ccdb (engines PR #213, ASC-1). 10 canonical paths, all modified. BEHAVIOUR CHANGES: isoCompliance.isAuditOverdue asks the new AUDIT_UNDELIVERED_STATUSES (Planned, In progress, Fieldwork complete), so a Reported audit is no longer overdue, as in auditManagement, and summarise().auditsOverdue follows (AUDIT_OPEN_STATUSES unchanged); new missingEvidenceParts(clause), and the unevidenced-claim readiness sentence names the missing parts; the Due soon explainStatus reason says 'inside the default 30 day lead time (none is set for this obligation)' when the default applies. Previous range: 6b00f4370e2090bcda4973626949c2dd458501d0..9d5d3b486493b8d08eb7dc38557d10e63e56adc2 (engines PR #212, ASC-0: the repairs the two Assurance courses found). 47 canonical paths (45 modified, 2 added: __tests__/assurance.copy.test.js and __tests__/assurance.instants.test.js). BEHAVIOUR CHANGES: risk isReviewOverdue reads a string as-of date as a calendar date and is true only for a RISK_LIVE_STATUSES risk; MOC isOverdue asks only before the change is on the facility (Draft to Approval), so a change in Implementation is not overdue and summarise().overdue and byUrgency follow; peer review gains REVIEWER_ROLES, canAssignPeerReviewer (the author is never a Lead Reviewer or Reviewer) and canActOnComment (the author never takes Verified, Rejected or Withdrawn), and summarise() skips comments on Closed or Cancelled reviews; refusal copy agrees in number, article and count; audit summarise().auditsOutstanding counts an unexplained cancellation as outstanding; every progress percent rounds half up on the exact rational; ISO certificateExpiring is 0 to 90 days only (a lapsed certificate is expired only), the ISO sentences name the register's standard and canSetClauseStatus takes the standard as a fourth argument; explainStatus prints 'Filed <date>. A one-off obligation, nothing further is due.' for a filed One-off; calendar gains localDateOf. Previous range: fdd6efe869abd829be92dcdd0a56b9ea948e3cc4..6b00f4370e2090bcda4973626949c2dd458501d0 (engines PR #211, AS15: the Assurance owner decisions). BEHAVIOUR CHANGES: MOC approvals and document review tasks are decided only by their assignee, and an originator/author never approves their own (canAssignApprover, canDecideApproval, canAssignReviewer, canDecideReviewTask); an Emergency change may be implemented on its first approval level and must be ratified within 7 days, and cannot close until it is (ratificationState; MOC summarise gains ratificationPending/ratificationOverdue and takes approvals); deriveStatus throws on an invalid today; evidence counts towards Compliant only for the current period (periodStart); fractional risk levels are unscored; ISO coverage counts only Reported/Closed audits, canExamineClause, an expired certificate is a serious readiness item; N/A without a reason is not an answer and a cancellation without a reason does not complete a programme. Previous range: 9600019c27c3cd8664c0d0c66eb42a760bb6a5bf..fdd6efe869abd829be92dcdd0a56b9ea948e3cc4 (engines PR #210, AS14: outstanding counts skip finished parents; canRemoveCheckpoint and canRaiseNcr). Re-pinned from the PR head 43f8cea to the squash merge commit (identical tree). 10 canonical paths. BEHAVIOUR CHANGES: MOC summarise() no longer counts actions on Closed, Rejected or Cancelled changes as open or overdue; QA summarise() no longer counts points on Closed, Superseded or Cancelled plans as outstanding, overdue or held, nor corrective actions on Voided NCRs as open, overdue or awaiting effectiveness (totals, failures and the effectiveness record unchanged; a child whose parent is not supplied still counts); new canRemoveCheckpoint(checkpoint, plan) and canRaiseNcr(plan). Previous range: 871d2f9..9600019c27c3cd8664c0d0c66eb42a760bb6a5bf (engines PR #209, AS13-0: three Assurance rule repairs before launch). BEHAVIOUR CHANGES: a filed One-off regulatory obligation is Compliant (it used to turn Overdue the day after its due date and stay in Needs attention); a HOLD point set to Not applicable needs a date, who decided and a reason (the waiver's record; it needed nothing); a closed temporary MOC reads the new EXPIRY.CLOSED_OUT, 'Closed out', where it read 'Permanent change' (the Suite shim adds its colour token). Previous range: 9874d5834de552a08ed7aa16e0fcd12438fefdb1..871d2f95634d0a0f29863a1fa6b768abbca365b2 (engines PRs #207 and #208: AS12, the new engines/assurance domain, and AS12b, clauseCoverageByStandard, which the ISO clause register and reports now call so no register falls back to the 3-year default). 52 canonical paths, all new except README.md: ten engine modules under engines/assurance/, ten ported rule-test suites plus assurance.goldens.test.js and its helper, ten goldens under test-data/assurance/goldens/, and ten oracles plus ten FINDINGS files under tools/validation/assurance/. The import closure is inside the domain (calendar.js, and isoCompliance -> qualityAssurance, auditManagement -> isoCompliance). BEHAVIOUR CHANGES for callers, all repairs the oracles found: an impossible date string ('2026-02-30') is now no date instead of a nearby real one; a risk review due today no longer reads overdue west of Greenwich; a blank residual axis falls back to the inherent level instead of scoring the residual 0 (so the Risk Register form preview and the saved appetite_status change for half-assessed risks); unreadable review and expiry dates no longer read due or expiring soon, and an unreadable expiry no longer passes the MOC implementation gate; ISO summarise() uses each standard's own certification cycle; closed and voided findings stop ageing at their closed date. Consumers are the nine src/lib Assurance modules, now re-export shims that keep their colour tokens."
  },
  "testing": {
    "$comment": "The Suite needs no equivalent of NextGen's tools/run-vendored-engine-tests.mjs. jest.config.js uses testMatch '**/__tests__/**/*.test.(js|jsx|ts)' with no testPathIgnorePatterns, so all 179 suites under packages/engines/__tests__ already run in `npm test` beside the Suite's own. There is no allow-list of known engine test failures because there are none: the vendored suites are green.",
    "runner": "the repo's own jest (npm test)",
    "knownTestFailures": {
      "suites": []
    }
  },
  "floors": {
    "manifestPaths": 750,
    "vendoredPaths": 750
  },
  "knownDeviations": []
}
